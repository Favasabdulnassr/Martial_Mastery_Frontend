version: '3.8'

services:
  frontend:
    build: .
    container_name: martial_mastery_frontend
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      # This anonymous volume ensures local node_modules don't overwrite the container's node_modules
      - /app/node_modules
    env_file:
      - .env
